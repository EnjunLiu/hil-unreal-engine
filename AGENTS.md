# UE5 端上下文规则

## 文档门禁与状态同步

开始任何 UE5、图像、数据采集、闭环或部署任务时，第一条动作必须完整读取三份 `WORKSPACE_CONTEXT.md`；读取前不得编辑、启动 UE5/ROS、采集数据或清理目录。对话摘要、旧工程和旧日志不能替代文档与现场核验。

每个任务只保留一个当前唯一目标和一个下一步唯一动作。任务开始、阶段完成、阻塞、暂停/上下文压缩前和结束时，立即更新三份交接文档；无变化的端也要注明无变化。状态使用“未开始/进行中/阻塞/已验收”，证据标记为现场验证、静态推断、历史记录或未验证。

开始任何 UE5、图像、数据采集、闭环或部署相关工作前，必须先阅读：

1. `D:\asv-unreal-simulation\WORKSPACE_CONTEXT.md`
2. `D:\asv-vla-training\WORKSPACE_CONTEXT.md`
3. `/home/jetson/jetson_asv_ws/WORKSPACE_CONTEXT.md`

`WORKSPACE_CONTEXT.md` 是 UE5 端当前状态的唯一事实入口。不要用对话摘要、旧 UE 工程或旧日志推断当前项目。

完成 UE5 代码修改、图像链路验证、数据采集、闭环测试、模型部署或目录清理后，必须立即更新交接文档，记录目标、进度、证据、路径、运行参数、阻塞项和下一步。三份文档冲突时先暂停并核对现场。
